# Sui wallet private key (suiprivkey1...)
SUI_PRIVATE_KEY=

# Dark pool contract addresses (set by scripts/create-pool.ts)
DARK_POOL_PACKAGE=
DARK_POOL_OBJECT=
ADMIN_CAP_ID=
MATCHER_CAP_ID=

# Seal encryption (optional - without this, runs in demo mode without order privacy)
SEAL_ALLOWLIST_ID=

# Backend config (optional)
PORT=3001
SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# TEE config (optional - defaults to local-dev)
TEE_MODE=local-dev
ENCLAVE_KEY_PATH=/app/ecdsa.sec

# Backend URL for test scripts
BACKEND_URL=http://localhost:3001
